(ns dimigi.logging.core-jvm
  (:require [taoensso.timbre :as timbre]
            [io.aviso.ansi :as ansi]
            ))



(defn- color-logger [options {:keys [level output-fn] :as data}]
  ;; removed
  )


(defn config-timbre! [options]
  ;; removed
  )


(defn configure-logging! [args]
  ;; removed
  )
